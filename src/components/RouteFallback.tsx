/** Quiet placeholder while a lazy route chunk loads. Same canvas as the rest of the site. */
const RouteFallback = () => (
  <div className="min-h-svh bg-background" aria-hidden />
);

export default RouteFallback;
